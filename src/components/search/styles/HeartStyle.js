import styled from "styled-components";

const HeartContainer = styled.div`
    position: relative;
    width: 5rem;
    height: 5rem;
    right: -1rem;
    top: -1.5rem;
    border-radius: 5rem;

    .lining {
        position: relative;
        top: -3.5rem;
        left: 1.69rem;
        width: 1.6rem;
        height: 1.7rem;
    }
}`;

export default HeartContainer;